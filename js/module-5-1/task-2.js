// инлайновы колбеки

// const numbers = [5, 7, 35, 85];

// function multiNums(numbers, callback, makeAlert) {
//     let total = 1;
//     numbers.forEach((number) => {
//         total *= number;
//     });
//     const message = callback(total);
//     const testMessange = makeAlert(message);

//     return testMessange;

// }



// console.log(multiNums(numbers, (result) => `Загальна сума добутку складає ${result}`, (message) => {
//     const name = prompt(`введить имя`);
//     const newMassage = `витаемо ${name}` + message;
//     return newMassage;
// }));



